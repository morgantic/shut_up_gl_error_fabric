{
  "schemaVersion": 1,
  "id": "${id}",
  "version": "${version}",
  "name": "${name}",
  "description": "Fork of Shut Up GL Errors by JamCoreModding for newer Fabric versions",
  "authors": [
    "Morgantic"
  ],
  "license": "CC0-1.0",
  "icon": "assets/shut_up_gl_error_fabric/icon.png",
  "environment": "*",
  "mixins": [
    "shut_up_gl_errors_fabric.mixins.json"
  ],
  "depends": {
    "fabricloader": ">=0.17",
    "minecraft": "${minecraft}"
  }
}